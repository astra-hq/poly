apiVersion: v1
kind: Service
metadata:
  name: {{ include "poly-kg.fullname" . }}-lightrag
  labels:
    {{- include "poly-kg.labels" . | nindent 4 }}
    app.kubernetes.io/component: lightrag
spec:
  type: {{ .Values.lightrag.service.type }}
  ports:
    - port: {{ .Values.lightrag.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "poly-kg.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: lightrag
